# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
sse-starlette>=2.0.0,<3.0.0

# LangChain & LangGraph (LangChain以频繁breaking change著称，严格锁上界)
langchain>=0.3.0,<0.4.0
langchain-openai>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.4.0,<0.5.0
langgraph-checkpoint-postgres>=2.0.0,<3.0.0

# LLM & Embeddings
zhipuai>=2.0.0,<3.0.0

# Database
psycopg[binary,pool]>=3.2.0,<4.0.0
sqlalchemy[asyncio]>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0

# Vector Store
pgvector>=0.3.0,<1.0.0

# Redis
redis>=5.0.0,<6.0.0

# Config & Utils
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0

# Auth
PyJWT>=2.8.0,<3.0.0

# Dev (可选)
# pytest>=8.0.0
# pytest-asyncio>=0.24.0
# ruff>=0.5.0

# ====== 语音网关 (AI电话客服) ======
# WebSocket服务器
websockets>=12.0,<14.0

# HTTP客户端(Agent API调用)
aiohttp>=3.9.0,<4.0.0

# 音频处理
numpy>=1.24.0,<2.0.0

# STT - 语音识别 (按需安装)
# funasr>=1.0.0          # FunASR流式中文识别

# TTS - 语音合成 (按需安装)
# edge-tts>=6.1.0        # 微软免费TTS(推荐首选)
# pydub>=0.25.1          # MP3→PCM转换(edge-tts备选)
# ffmpeg-python>=0.2.0   # ffmpeg封装(音频格式转换)

# VAD - 语音端点检测 (按需安装)
# torch>=2.0.0           # Silero VAD依赖
# webrtcvad>=2.0.10      # WebRTC VAD(轻量方案)

# CosyVoice - 高品质TTS (可选，需GPU)
# 需要单独安装: https://github.com/FunAudioLLM/CosyVoice
